services:
  fraud-api:
    build:
      context: ../..
      dockerfile: deployment/docker/Dockerfile
    container_name: fraud-api
    ports:
      - "8000:8000"
    environment:
      FRAUD_MODEL_ARTIFACT_PATH: /app/artifacts/best_fraud_model_f1.joblib
